# __AGENT_ID__ — recursive agent, level __LEVEL__

This is a **self-contained recursive agent** of the adaptive chain, built
strictly per **IPP v0.2.8**. It was **generated by the recursive compiler**
(`recursive_agents/runtime/compiler.py`) from the templates in
`recursive_agents/runtime/templates/` — the "first compilation" produced
`agent_a1`; every later agent was produced by the previous agent's own
tools node.

## Situation

| | |
|:---|:---|
| **Agent** | `__AGENT_ID__` (level __LEVEL__) |
| **Engine node** | `__AGENT_ID__/__AGENT_ID___engine` — `ground` / `chat` / `chat_stream` (internal edge `ground → chat`) |
| **Tools node** | `__AGENT_ID__/__AGENT_ID___tools` — the agent-construction suite + the shared capability surface |
| **Generated by** | `__GENERATED_BY__` via its tools node |

## The agent's hands — `__AGENT_ID___tools`

The tools node carries the **agent-construction tool suite** with which
this agent thinks, evaluates and self-acts to generate the **next**
recursive agent:

| Tool | What it does |
|:---|:---|
| `agent_plan` | THINK — the deterministic construction plan for the next agent |
| `agent_generate` | GENERATE — render the templates → the next agent's engine + tools folders |
| `agent_create` | CREATE — compile + construct (Γ, 7-step protocol) + verify ALL 17 invariants |
| `agent_evaluate` | EVALUATE — 17 invariants + audit chains + channel surface |
| `agent_test` | TEST — ground→chat pipeline through the envelopes + latency probe → feedback |
| `agent_improve` | IMPROVE — test → deterministic patch → retest until it passes |
| `agent_deploy` | DEPLOY — generate + construct + register in the chain |
| `agent_status` | report the constructed chain |

Plus `invoke` / `list` / `describe` over the **shared tools node**
(`general_tools`) for the general capability surface (read_file,
web_search, get_local_graph, …) — one execution plane, one audit trail.

## How to drive it

```python
from recursive_agents import AgentCompiler
from general_tools.graph import KnowledgeGraph
from general_tools.encoder import EncoderLayer

compiler = AgentCompiler(KnowledgeGraph(auto_load=False), EncoderLayer())
engine = compiler.constructed["__AGENT_ID__"]      # after construction

# instruct the agent (chat → LLM function calling; the LLM calls agent_*)
answer, trace = engine.run_with_trace(
    "Create the next recursive agent and improve it with feedback.")

# or drive its tools directly (each call through the guardrail envelope)
engine._tools_node.invoke("agent_plan", {"agent_id": "agent_aX"})
engine._tools_node.invoke("agent_create", {"agent_id": "agent_aX"})
```

## Strict IPP v0.2.8

- Every call flows through the guardrail envelope
  ι_pre → π → Ω → ι_post → ρ → τ* with hash-chained audit records.
- ALL 17 invariants verified on construction (see `agent_evaluate`).
- The tools node's `invoke` channel declares its downstream edge to the
  shared tools node — constructor-resolved in 𝒢.
- Audit logs: `recursive_agents/graph_data/logs/IPP/<node>.<channel>.jsonl`.
