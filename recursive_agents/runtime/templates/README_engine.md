# __AGENT_ID___engine — the engine IPP node of __AGENT_ID__

Generated by the recursive compiler from the engine template. Three
channels, each a complete object-executor pair (Ω + Ξ) with its own
guardrails and hash-chained audit log:

| Channel | Handler | Process |
|:---|:---|:---|
| `ground` | `make_ground_handler` | materialize the depth-3 local graph + encoder evidence → the grounded task |
| `chat` | `make_chat_handler` | the full agent loop (LLM function calling over the agent_* suite + the shared tools node) → `{answer, trace, tokens}` |
| `chat_stream` | `make_chat_stream_handler` | the observable loop as a collected event stream |

**Internal topology:** one blocking internal edge `ground → chat`
(I15/I16/I17 — port-level DAG, payload-only).

**The engine is the loop; the tools are the hands.** The agent-construction
capability (`agent_create`, `agent_generate`, `agent_evaluate`,
`agent_test`, `agent_improve`, `agent_deploy`, `agent_plan`,
`agent_status`) lives in the agent's tools node
(`recursive_agents/__AGENT_ID__/__AGENT_ID___tools`).

Files in this folder:

- `IPP.json` — the F-file (node_id `__NODE_ID__`), ports, guardrails,
  capability spaces, internal topology
- `IPP_object.py` — the Ω handler factories (bound by Γ to the live
  engine through GraphContext.bindings)
- `IPP_executor.py` — the Ξ class (audit extras: trace steps, tool calls)
- `__init__.py` — package marker
- `README.md` — this file

Constructed by Γ (the recursive compiler) with the 7-step protocol:
objects → guards → external topology → internal wiring → return.
