# __AGENT_ID___tools — the tools IPP node of __AGENT_ID__

Generated by the recursive compiler from the tools template. This is the
agent's **hands**: the agent-construction tool suite with which this
agent thinks, evaluates and self-acts to generate the **next** recursive
agent — plus the general capability surface over the shared tools node.

## Channels (11)

| Channel | Handler | What it does |
|:---|:---|:---|
| `invoke` | `make_invoke_handler` | execute an ACL tool through the SHARED tools node's envelope (ACL enforced here) |
| `list` | `make_list_handler` | the full tool surface: agent_* suite + ACL names |
| `describe` | `make_describe_handler` | one tool's definition (agent_* local, ACL from the shared F-file catalog) |
| `agent_plan` | `make_agent_plan_handler` | THINK — the deterministic construction plan |
| `agent_generate` | `make_agent_generate_handler` | GENERATE — render templates → the next agent's folders |
| `agent_create` | `make_agent_create_handler` | CREATE — compile + construct (Γ) + verify ALL 17 invariants |
| `agent_evaluate` | `make_agent_evaluate_handler` | EVALUATE — invariants + audit chains + channels |
| `agent_test` | `make_agent_test_handler` | TEST — pipeline through the envelopes + latency probe → feedback |
| `agent_improve` | `make_agent_improve_handler` | IMPROVE — test → deterministic patch → retest |
| `agent_deploy` | `make_agent_deploy_handler` | DEPLOY — generate + construct + register in the chain |
| `agent_status` | `make_agent_status_handler` | report the constructed chain |

The construction machinery is the shared **AgentCompiler** bound by Γ
(GraphContext.bindings["compiler"]) — this agent's hands over it.

## Strict IPP v0.2.8

- Every channel flows through the guardrail envelope
  ι_pre → π → Ω → ι_post → ρ → τ* with hash-chained audit records; the
  audit extras record the tool identity + the target agent.
- The `invoke` channel declares its downstream edge to the shared tools
  node (`node_class: "tools"`) — constructor-resolved in 𝒢.
- ALL 17 invariants verified on construction.

Files in this folder:

- `IPP.json` — the F-file (node_id `__NODE_ID__`)
- `IPP_object.py` — the Ω handler factories
- `IPP_executor.py` — the Ξ class
- `__init__.py` — package marker
- `README.md` — this file
